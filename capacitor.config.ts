import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'arosaje.mspr.antd',
  appName: 'arosaje-front',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
